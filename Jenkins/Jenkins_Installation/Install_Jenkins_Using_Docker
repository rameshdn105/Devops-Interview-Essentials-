Pull the Jenkins Docker image:
docker pull jenkins/jenkins:lts

Run Jenkins in a Docker container:
docker run -d -p 8080:8080 -p 50000:50000 --name jenkins jenkins/jenkins:lts

Access Jenkins UI:

Jenkins will be available at http://localhost:8080 in your browser.
Unlock Jenkins:

Get the unlock key by checking the logs of the running container:
docker logs jenkins


