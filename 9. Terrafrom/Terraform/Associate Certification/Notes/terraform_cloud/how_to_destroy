
you have to options for destroy

1, destroy the resources
2, destroy resources + workspace

to do so,

go to workspace -> setting -> Destruction and Deletion -> here you have two options -> manually destroy and destroy workspace

